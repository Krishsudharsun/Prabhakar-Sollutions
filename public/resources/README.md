Drop your real PDF here as `operations-audit-checklist.pdf` — the Resources
page already links to `/resources/operations-audit-checklist.pdf`, so the
download button will start working the moment the file is added, no code
changes needed. Delete this README once the real PDF is in place.
